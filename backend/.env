DATABASE_URL=postgresql://postgres.lpqgaszqomhsicbhikds:Vishnu_bharathi@aws-0-ap-northeast-2.pooler.supabase.com:5432/postgres
JWT_SECRET=replace-with-a-long-random-secret
PORT=5000
FRONTEND_URL=http://localhost:5173

# --- Email notifications (optional but recommended) ---
# If these are left blank, the app still works normally — it just logs
# what would have been emailed instead of actually sending it.
#
# Example values for Gmail (use an App Password, not your normal password):
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=youraddress@gmail.com
#   SMTP_PASS=your-16-char-app-password
#
# Any standard SMTP provider works too (SendGrid, Mailgun, Postmark, Office365, etc).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=harsha970160@gmail.com
SMTP_PASS=wnlj veso xxbf ebew
SMTP_FROM=Service Desk <harsha970160@gmail.com>